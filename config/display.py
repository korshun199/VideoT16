"""Настраиваемый внешний вид OSD и подписей объектов.

Цвета указаны в формате BGR, который использует OpenCV:
    (синий, зелёный, красный).

После изменения файла программу нужно перезапустить.
"""

# Настройки текста телеметрии INAV.
OSD_TEXT = {
    # Название шрифта OpenCV: FONT_HERSHEY_SIMPLEX, FONT_HERSHEY_PLAIN и другие.
    "font": "FONT_HERSHEY_SIMPLEX",
    # Размер текста: 0.4 — маленький, 0.7 — крупнее.
    "font_scale": 0.5,
    # Жёлтый цвет текста в формате BGR.
    "color": (0, 255, 255),
    # Чёрная тень текста.
    "shadow_color": (0, 0, 0),
    # Толщина основного текста.
    "thickness": 1,
    # Толщина тени.
    "shadow_thickness": 2,
    # Смещение тени: вправо и вниз.
    "shadow_offset": (1, 1),
    # Положение первой строки и расстояние между строками.
    "left": 20,
    "top": 30,
    "line_spacing": 24,
}

# Настройки синей строки температуры и загрузки CPU для пилота.
SYSTEM_STATUS_STYLE = {
    "font": "FONT_HERSHEY_SIMPLEX",
    "font_scale": 0.5,
    "color": (255, 0, 0),
    "shadow_color": (0, 0, 0),
    "thickness": 1,
    "shadow_thickness": 2,
    "shadow_offset": (1, 1),
    "left": 20,
    "bottom": 18,
}

# Настройки состояния ARM и вертикального индикатора газа справа.
FLIGHT_STATUS_STYLE = {
    # Отступ блока от правого и верхнего края кадра.
    "right": 40,
    "top": 30,
    # Расстояние между строками ARM и THR.
    "line_spacing": 24,
    # Положение и размер вертикальной шкалы газа.
    "bar_top_offset": 36,
    "bar_width": 18,
    "bar_height": 120,
    # Цвет рамки и заполнения шкалы в формате BGR.
    "bar_color": (0, 255, 255),
    "bar_thickness": 1,
    "fill_color": (0, 255, 255),
    # Чёрная окантовка шкалы сохраняет контраст на светлом видео.
    "shadow_color": (0, 0, 0),
    "shadow_thickness": 3,
    # Белый подвижный маркер показывает текущее положение стика газа.
    "marker_color": (255, 255, 255),
    "marker_shadow_color": (0, 0, 0),
    "marker_thickness": 1,
    "marker_shadow_thickness": 4,
    "marker_overhang": 4,
}

# Крупная нижняя надпись ARM OFF или текущего режима полёта.
ARM_BANNER_STYLE = {
    # Шрифт и размер общей нижней надписи.
    "font": "FONT_HERSHEY_SIMPLEX",
    "font_scale": 1.0,
    # ARM OFF мигает красным, активный режим отображается зелёным.
    "disarmed_color": (0, 0, 255),
    "armed_color": (0, 255, 0),
    # Чёрная окантовка сохраняет читаемость на светлом видео.
    "shadow_color": (0, 0, 0),
    "thickness": 2,
    "shadow_thickness": 6,
    "shadow_offset": (2, 2),
    # Отступ базовой линии текста от нижнего края кадра.
    "bottom_margin": 30,
    # Полный период мигания ARM OFF в секундах.
    "blink_period": 1.0,
}

# Настройки рамки и подписи обнаруженного объекта.
OBJECT_STYLE = {
    # Цвет рамки и подписи OBJECT в формате BGR.
    "color": (0, 255, 0),
    # Толщина рамки объекта.
    "box_thickness": 2,
    # Шрифт подписи объекта.
    "font": "FONT_HERSHEY_SIMPLEX",
    "font_scale": 0.7,
    "text_thickness": 2,
    # Минимальная высота подписи от верхнего края кадра.
    "text_min_y": 20,
    "text_offset_y": 8,
}

# Настройки движущейся полоски горизонта.
HORIZON_STYLE = {
    # Сколько пикселей приходится на один градус тангажа.
    "pitch_scale": 4.0,
    # Ограничение смещения горизонта вверх и вниз в градусах.
    "pitch_limit": 45.0,
    # Минимальная длина горизонта в пикселях.
    "min_length": 240,
    # Доля меньшей стороны кадра, используемая для длины горизонта.
    "length_ratio": 0.5,
    # Цвет и толщина основной линии горизонта.
    "color": (0, 255, 255),
    "thickness": 1,
    # Чёрная подложка, чтобы линия была видна на фоне видео.
    "shadow_color": (0, 0, 0),
    "shadow_thickness": 3,
}

# Настройки неподвижной центральной оси самолёта.
AXIS_STYLE = {
    # Половина длины вертикальной оси и горизонтальных крыльев.
    "vertical_half_length": 50,
    "horizontal_half_length": 64,
    # Цвет и толщина основной оси.
    "color": (255, 255, 255),
    "thickness": 1,
    # Чёрная подложка оси.
    "shadow_color": (0, 0, 0),
    "shadow_thickness": 3,
}
